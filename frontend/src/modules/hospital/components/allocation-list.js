import { formatTime } from './request-status.js';
export function hospitalAllocationList(items){const section=document.createElement('section');const h=document.createElement('h3');h.textContent='Allocated by blood banks';section.append(h);if(!items.length){const p=document.createElement('p');p.textContent='No bank allocations yet.';section.append(p);return section;}const table=document.createElement('table');const head=document.createElement('tr');for(const label of['Blood bank','Units','Status','Reserved','Released','Completed']){const th=document.createElement('th');th.textContent=label;head.append(th);}table.append(head);for(const a of items){const tr=document.createElement('tr');for(const value of[a.bank.name,String(a.unitsReserved),a.status,formatTime(a.reservedAt),a.releasedAt?formatTime(a.releasedAt):'-',a.completedAt?formatTime(a.completedAt):'-']){const td=document.createElement('td');td.textContent=value;tr.append(td);}table.append(tr);}section.append(table);return section;}
