'use strict';const{NotFoundError,ConflictError}=require('../../core/errors');function requireProfile(row){if(!row)throw new NotFoundError('Donor profile not found.',{code:'DONOR_PROFILE_NOT_FOUND'});return row;}function duplicate(){return new ConflictError('A donor profile already exists for this account.',{code:'DONOR_PROFILE_ALREADY_EXISTS'});}module.exports={requireProfile,duplicate};
