'use strict';const r=require('express').Router();const{requireRole}=require('../../middleware/require-role');const{requireActive}=require('../../middleware/require-active');const{validate}=require('../../middleware/validate');const{ROLES}=require('../../core/constants');const sc=require('./donors.schemas');const c=require('./donors.controller');r.use(requireRole(ROLES.DONOR),requireActive);r.post('/profile',validate(sc.createProfileSchema),c.create);r.get('/profile',c.get);r.patch('/profile',validate(sc.updateProfileSchema),c.update);r.patch('/availability',validate(sc.availabilitySchema),c.availability);module.exports=r;
