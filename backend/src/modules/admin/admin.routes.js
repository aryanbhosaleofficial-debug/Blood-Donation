'use strict';const r=require('express').Router();const {requireRole}=require('../../middleware/require-role');const {validate}=require('../../middleware/validate');const {ROLES}=require('../../core/constants');const {userIdSchema}=require('./verification.schemas');const c=require('./admin.controller');r.use(requireRole(ROLES.ADMIN));r.get('/organizations/pending',c.pending);r.get('/organizations/verified',c.verified);r.post('/organizations/:userId/verify',validate(userIdSchema,'params'),c.verify);r.post('/organizations/:userId/revoke',validate(userIdSchema,'params'),c.revoke);module.exports=r;
