'use strict';const config=require('../../core/config');const{ConflictError,NotFoundError}=require('../../core/errors');const{compatibleDonorGroups}=require('./compatibility');const{filterAndRankPotentialDonors}=require('./donor-filter.service');const repo=require('./matching.repository');const alertsRepo=require('../donor-alerts/donor-alerts.repository');const serializer=require('./matching.serializer');const{getDb}=require('../../core/database');const logger=require('../../core/logger');
function activatePotentialDonorFallback(user,requestId,now=Date.now()){const request=repo.ownedOpenRequest(requestId,user.id);if(!request)throw new NotFoundError('Request not found.',{code:'REQUEST_NOT_FOUND'});const remaining=Math.max(request.units_needed-request.bank_units_allocated,0);if(request.status!=='OPEN'||remaining<=0)throw new ConflictError('Potential donor fallback is not required for this request.',{code:'DONOR_FALLBACK_NOT_REQUIRED'});const groups=compatibleDonorGroups(request.component,request.blood_group);const cutoff=new Date(now-config.availabilityFreshnessDays*86400000).toISOString();const rows=repo.candidatesForRequest(requestId,groups,cutoff,new Date(now).toISOString(),config.donorMatchLimit*20);const hospital={city:request.h_city,locality:request.h_locality,pin_code:request.h_pin_code,latitude:request.h_latitude,longitude:request.h_longitude};const selected=filterAndRankPotentialDonors(rows,hospital,config.donorMatchLimit);const write=getDb().transaction(()=>{const current=repo.ownedOpenRequest(requestId,user.id);const currentRemaining=current?Math.max(current.units_needed-current.bank_units_allocated,0):0;if(!current)throw new NotFoundError('Request not found.',{code:'REQUEST_NOT_FOUND'});if(current.status!=='OPEN'||currentRemaining<=0)throw new ConflictError('Potential donor fallback is not required for this request.',{code:'DONOR_FALLBACK_NOT_REQUIRED'});let created=0;for(const item of selected)created+=alertsRepo.assignOrReactivate(getDb(),requestId,item.row.id).changes;return{created,total:alertsRepo.countActionable(getDb(),requestId)};});const result=write.immediate();logger.info('potential donor fallback activated',{requestId,candidateCount:selected.length,newAlertCount:result.created});return serializer.hospitalFallbackResult(requestId,result.created,result.total);}
module.exports={activatePotentialDonorFallback};
