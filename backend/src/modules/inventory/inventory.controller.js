'use strict';const {sendSuccess}=require('../../core/response');const s=require('./inventory.service');function list(req,res,next){try{return sendSuccess(res,{inventory:s.list(req.user.id)});}catch(e){return next(e);}}function update(req,res,next){try{return sendSuccess(res,{inventory:s.update(req.user.id,req.inventoryId,req.validated)});}catch(e){return next(e);}}module.exports={list,update};
