'use strict';const r=require('express').Router();const {validate}=require('../../middleware/validate');const sc=require('./inventory.schemas');const c=require('./inventory.controller');r.get('/',c.list);r.patch('/:inventoryId',validate(sc.inventoryIdSchema,'params'),(req,res,next)=>{req.inventoryId=req.validated.inventoryId;next();},validate(sc.updateInventorySchema),c.update);module.exports=r;
