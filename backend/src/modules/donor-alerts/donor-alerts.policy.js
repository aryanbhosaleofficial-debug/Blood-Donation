'use strict';const{NotFoundError,ConflictError}=require('../../core/errors');function owned(row){if(!row)throw new NotFoundError('Alert not found.',{code:'DONOR_ALERT_NOT_FOUND'});return row;}function transition(row,allowed){if(!allowed.includes(row.status))throw new ConflictError('The alert cannot change from its current state.',{code:'INVALID_DONOR_ALERT_STATE'});}module.exports={owned,transition};
