'use strict';require('../helpers/env');const{test}=require('node:test');const assert=require('node:assert/strict');const{haversineDistanceKm}=require('../../src/modules/matching/distance.service');test('Haversine returns approximate great-circle distance',()=>{assert.ok(Math.abs(haversineDistanceKm(0,0,0,1)-111.2)<0.5);assert.equal(haversineDistanceKm(23.03,72.56,23.03,72.56),0);});
