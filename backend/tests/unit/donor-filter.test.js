'use strict';require('../helpers/env');const{test}=require('node:test');const assert=require('node:assert/strict');const{locationScore,filterAndRankPotentialDonors}=require('../../src/modules/matching/donor-filter.service');const h={latitude:23.03,longitude:72.56,pin_code:'380009',locality:'Navrangpura',city:'Ahmedabad'};test('coordinates enforce radius and fallback tiers are deterministic',()=>{assert.equal(locationScore({approx_latitude:23.04,approx_longitude:72.57},h).tier,0);assert.equal(locationScore({approx_latitude:30,approx_longitude:80},h),null);const noCoords={approx_latitude:null,approx_longitude:null};assert.equal(locationScore({...noCoords,pin_code:'380009'},h).tier,1);assert.equal(locationScore({...noCoords,locality:'navrangpura'},h).tier,2);assert.equal(locationScore({...noCoords,city:'AHMEDABAD'},h).tier,3);assert.equal(locationScore({...noCoords,city:'Surat'},h),null);});test('ranking is bounded and prioritizes logistics tier',()=>{const rows=[{id:2,pin_code:'380009',availability_updated_at:'2026-01-01Z'},{id:1,approx_latitude:23.031,approx_longitude:72.561,availability_updated_at:'2026-01-01Z'}];assert.equal(filterAndRankPotentialDonors(rows,h,1)[0].row.id,1);});
